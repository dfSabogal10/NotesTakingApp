# Notes Taking App

## Overview

(Placeholder: high-level description of the app.)

## Tech Stack

(Placeholder: backend and frontend technologies.)

## Local Setup

### Backend (Docker)

1. Copy the backend environment example:
   - `cp backend/.env.example backend/.env`
2. Build and start the services:
   - `docker compose up --build`

### Frontend

1. Install dependencies:
   - `cd frontend && npm install`
2. Run the dev server:
   - `npm run dev`

## Decisions

(Placeholder: notable architecture or tooling decisions.)
